- name: configuration mysql
  hosts: mysql
  become: yes
  tasks: 
  - name: install mysql server
    ansible.builtin.package:
      name: mysql-server
      state: installed

  - name: start service
    ansible.builtin.service:
      name: mysqld
      state: started
      enabled: yes